/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['avatars.githubusercontent.com', 'df-frontend-ass4.vercel.app'],
  },
}

module.exports = nextConfig
